{
  "name": "ai-message",
  "version": "0.0.1",
  "private": true,
  "description": "Chat Analysis platform — анализ коммуникационных каналов с интеграцией Bitrix24",
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "dev": "pnpm --filter @ai-message/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
